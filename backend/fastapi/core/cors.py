# Allowed origins for CORS middleware add more origins as needed
origins = [
  "http://localhost:3000",
  "http://localhost:8000",
  "http://localhost:8080",
]

# Allowed trusted hosts for CORS middleware add more trusted hosts as needed
trusted_hosts = [
  "localhost",
  "127.0.0.1",
  "testserver",
]
